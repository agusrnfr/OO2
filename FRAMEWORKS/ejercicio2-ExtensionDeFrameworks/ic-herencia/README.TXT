Ejemplo minimalista de Framework en el que se utiliza template method y herencia para implementar los hotspots.

La clase SimpleGame (en el paquete example), instancia el framework para construir un juego simple con solo dos robots.